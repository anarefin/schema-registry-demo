<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.example</groupId>
    <artifactId>schema-registry-demo-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <!-- T-2.7: JSON Schema contracts jar. Carries CustomerRegistered schema (Jackson) plus its
       own AMQP routing/topology auto-configuration (spring-amqp + spring-boot-autoconfigure),
       self-contained with no dependency on schema-messaging-core. -->
  <artifactId>customer-contracts</artifactId>
  <packaging>jar</packaging>

  <properties>
    <!-- Override on CLI: ./mvnw apicurio-registry:register -Dapicurio.registry.url=http://host:8080 -->
    <apicurio.registry.url>http://localhost:8080</apicurio.registry.url>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <!-- AMQP routing/topology for CustomerRegistered (self-contained, no dependency on schema-messaging-core) -->
    <dependency>
      <groupId>org.springframework.amqp</groupId>
      <artifactId>spring-amqp</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- T-2.2: Generate CustomerRegistered POJO from customer-registered.json (Draft 2020-12). -->
      <plugin>
        <groupId>org.jsonschema2pojo</groupId>
        <artifactId>jsonschema2pojo-maven-plugin</artifactId>
        <version>${jsonschema2pojo-maven-plugin.version}</version>
        <configuration>
          <sourceDirectory>${project.basedir}/src/main/resources/schemas</sourceDirectory>
          <targetPackage>com.example.contracts.customers</targetPackage>
          <annotationStyle>jackson2</annotationStyle>
          <sourceType>jsonschema</sourceType>
          <generateBuilders>true</generateBuilders>
          <includeHashcodeAndEquals>true</includeHashcodeAndEquals>
          <includeToString>true</includeToString>
          <useTitleAsClassname>true</useTitleAsClassname>
          <dateTimeType>java.lang.String</dateTimeType>
          <initializeCollections>false</initializeCollections>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- T-2.4 / T-4.1: Register CustomerRegistered. The COMPATIBILITY rule for this
           JSON Schema artifact is FORWARD (not BACKWARD): Apicurio's JSON Schema checker
           classifies adding any property as OBJECT_TYPE_PROPERTY_SCHEMAS_NARROWED, which
           BACKWARD rejects but FORWARD accepts. The rule is attached via REST/bootstrap.
           Run: ./mvnw -pl customer-contracts apicurio-registry:register
           Override URL: -Dapicurio.registry.url=http://host:8080
           v3 (input1 optional property) is a FORWARD-compatible addition. -->
      <plugin>
        <groupId>io.apicurio</groupId>
        <artifactId>apicurio-registry-maven-plugin</artifactId>
        <version>${apicurio-registry-maven-plugin.version}</version>
        <configuration>
          <registryUrl>${apicurio.registry.url}</registryUrl>
          <artifacts>
            <!-- v1: fields without promoCode — registered first to establish the baseline version. -->
            <artifact>
              <groupId>events.customers</groupId>
              <artifactId>CustomerRegistered</artifactId>
              <file>${project.basedir}/src/test/resources/schemas/customer-registered-v1.json</file>
              <artifactType>JSON</artifactType>
              <ifExists>FIND_OR_CREATE_VERSION</ifExists>
            </artifact>
            <!-- latest: src/main schema carries optional promoCode (v2) + input1 (v3),
                 each a FORWARD-compatible property addition. -->
            <artifact>
              <groupId>events.customers</groupId>
              <artifactId>CustomerRegistered</artifactId>
              <file>${project.basedir}/src/main/resources/schemas/customer-registered.json</file>
              <artifactType>JSON</artifactType>
              <ifExists>FIND_OR_CREATE_VERSION</ifExists>
            </artifact>
          </artifacts>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- T-4.5 CI merge gate: checks the current schema is compatible (FORWARD for this
         JSON artifact) with what is registered. Requires a running registry. Run during CI
         after docker compose up:
         ./mvnw -pl customer-contracts verify -Pcompat-check
         Or explicitly: ./mvnw -pl customer-contracts apicurio-registry:test -Pcompat-check -->
    <profile>
      <id>compat-check</id>
      <build>
        <plugins>
          <plugin>
            <groupId>io.apicurio</groupId>
            <artifactId>apicurio-registry-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>compat-gate</id>
                <phase>verify</phase>
                <goals><goal>register</goal></goals>
                <configuration>
                  <registryUrl>${apicurio.registry.url}</registryUrl>
                  <dryRun>true</dryRun>
                  <artifacts>
                    <artifact>
                      <groupId>events.customers</groupId>
                      <artifactId>CustomerRegistered</artifactId>
                      <file>${project.basedir}/src/main/resources/schemas/customer-registered.json</file>
                      <artifactType>JSON</artifactType>
                    </artifact>
                  </artifacts>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!-- T-4.4 / T-4.6 incompatible demo: runs apicurio-registry:test with a schema that
         adds a NEW REQUIRED field 'accountType' — incompatible under the FORWARD rule
         (an old reader cannot satisfy a newly-required field).
         This MUST fail with a clear compatibility rejection message.
         Demo: ./mvnw -pl customer-contracts verify -Pincompatible-demo -Dapicurio.registry.url=http://localhost:8080
         (requires a prior version already registered with the FORWARD rule) -->
    <profile>
      <id>incompatible-demo</id>
      <build>
        <plugins>
          <plugin>
            <groupId>io.apicurio</groupId>
            <artifactId>apicurio-registry-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>incompatible-compat-check</id>
                <phase>verify</phase>
                <goals><goal>register</goal></goals>
                <configuration>
                  <registryUrl>${apicurio.registry.url}</registryUrl>
                  <dryRun>true</dryRun>
                  <artifacts>
                    <artifact>
                      <groupId>events.customers</groupId>
                      <artifactId>CustomerRegistered</artifactId>
                      <file>${project.basedir}/src/test/resources/schemas/customer-registered-incompatible.json</file>
                      <artifactType>JSON</artifactType>
                    </artifact>
                  </artifacts>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
